// String (will create a simple string).
// Number(will create a value and return 0).
// Boolean (will create and retunr the value of false).
// BigInt(Command that requires a initial value to it).

const str = String(); 
const num = Number();
const bool = Boolean(); 

console.log(str); 
console.log(num);
console.log(bool);

const big1 = BigInt(42); 
console.log(big1);

const big2 = BigInt();
